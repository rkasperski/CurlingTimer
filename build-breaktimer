set -x
if [ ! -d ".venvb" ]
then
  sudo apt install git
  python -m venv .venvb
  source .venvb/bin/activate
  pip install -r breaktimer.requirements.txt
else
  source .venvb/bin/activate
fi

python build_install.py -v --breaktimer
